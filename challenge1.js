// Using const for values that won't change
const fullName = "Alex Rivera";
const age = 24;
const favLanguage = "JavaScript";
const isStudent = true;
const currentYear = 2025;

// Creating a formatted introduction using a template literal
const introduction = `Hello! My name is ${fullName}. I am ${age} years old, and my favorite programming language is ${favLanguage}
Am I currently a student? ${isStudent}
We are currently in the year ${currentYear}`;

console.log(introduction);

